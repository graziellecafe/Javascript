/*
Exercício 01: Para que a divisão entre dois números possa ser realizada, o divisor não pode ser nulo (zero). Escreva um programa para ler 2 valores e imprimir o resultado da divisão do primeiro pelo segundo. O programa deve validar a leitura do segundo valor (que não deve se nulo). Enquanto não for fornecido um valor a leitura deve ser repetida. Utilize a estrutura while na construção da repetição de validação.
*/

function divisaoDoisNumeros(numerador, denominador){
    if (numerador > denominador) {

    }
}

divisaoDoisNumeros(2,1)